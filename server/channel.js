// Channel object definition

function Channel(name,open) {
    var name = name;    // displayed name of the channel (char)
    var id = '';        // id of the channel (char)
    var open = open     // is the channel open for watchers ? (true or false)
}

module.exports = Channel;
